export interface IUser {
  _id: string;
  name: string;
  email: string;
  clerkUserID: string;
  isAdmin: boolean;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
  profilePic: string;
}
